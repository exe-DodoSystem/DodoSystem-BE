name: CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore SMEFLOWSystem.sln

    - name: Build
      run: dotnet build SMEFLOWSystem.sln --no-restore -c Release

  deploy:
    needs: build-and-test
    if: github.repository != '' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
    - name: Deploy Placeholder
      run: |
        echo "Cấu hình Script Deploy tự động tới VPS hoặc Azure App Service tại đây."
        echo "Nếu xài docker-compose, bạn sẽ ssh vào server và gõ: docker-compose up -d --build"
